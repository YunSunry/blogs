# DX-SMART LoRa 模块无法使用排查指南

如果您在使用或烧录 LoRa 模块时遇到问题，请按照以下步骤逐步排查。

---

## 步骤 1：确认您的开发板版本

在开始配置前，请先确认您手中的开发板硬件版本（**PJ23** 或 **PJ26**），不同版本对应的 RTS/DTR 控制模式不同。
* **如何识别：** 请根据下方图片对比您的开发板外观：

---

## 步骤 2：配置 MCUISP 烧录工具

打开 **MCUISP** 软件，确保以下 **4 处**设置完全正确：

1. **自动识别串口：** 在选择任何参数（如波特率）之前，**请先点击软件上方的 `EnumPort` 选项**。系统会自动刷新并识别当前可用的串口。如果点击后下拉列表中没有出现任何端口，请尝试更换电脑 USB 插口或检查数据线。
2. **串口与波特率：** 在识别出的列表中选择正确的 **COM 端口**，并将右侧的波特率设置为 **`460800`**。
3. **固件路径：** 加载您需要烧录的正确 `.hex` 文件。
4. **RTS/DTR 控制：** 点击左下角的下拉菜单，根据您的板子版本选择对应模式：
   * **如果是 PJ23 板子：** 请选择 `Reset@DTR Low(<-3V), ISP@RTS High`
   * **如果是 PJ26 板子：** 请选择 `Reset@DTR High(>+3V), ISP@RTS Low`（如下图所示）



---

## 步骤 3：读取芯片信息

1. 点击 MCUISP 的 **"Read ChipInfo(R)"** 按钮。
2. **关键操作：** 紧接着迅速按下开发板上的 **RST 复位键**，以触发芯片进入下载模式。

* **如果成功：** 芯片信息会打印在窗口中，说明硬件连接与烧录设置完全正常。
* **如果失败（出现错误提示）：** 请继续执行 **步骤 4** 进行模块独立串口测试。

---

## 步骤 4：模块独立串口测试（验证程序是否存在）

如果烧录工具无法读取芯片，我们需要通过普通串口软件测试模块内是否有现成程序、以及它是否能正常启动工作。

1. 关闭 MCUISP 软件，打开普通的 **串口调试助手**。
2. 配置串口参数：**波特率 `9600`**，数据位 `8`，停止位 `1`，校验位 `NONE`。
3. 按下板子上的 **RST 复位键**，观察数据接收窗口。

### 结果分析：
* **情况 A（模块有程序）：** 如果按下 RST 键后，窗口打印出 `---> Power On`、版本号（如 `---> V1.2.35`）或模块型号（如 `---> DX SX1262...`）等响应信息，说明**模块内部已有完整程序且本地运行正常**。请重新检查您的烧录软件设置或 USB 数据线。
* **情况 B（模块无程序/无响应）：** 如果多次按下 RST 键后，窗口完全没有任何数据返回（一片空白），说明**芯片内可能没有程序或处于深死机状态**。请返回 **步骤 2**，重点检查 RTS/DTR 硬件流控制是否与您的板子型号（PJ23/PJ26）严格匹配。

---

## 📞 联系我们

如果您在尝试了以上所有排查步骤后，模块依然无法正常使用，请联系我们的技术支持团队。为了让我们能更快速、精准地为您解决问题，**请提供以下相关信息**：

1. **产品信息：** 您所购买的 LoRa 模块型号。
2. **开发板型号：** 请注明您使用的是哪款开发板（**PJ23** 还是 **PJ26**）。
3. **具体问题描述：** 详细描述您在哪个步骤遇到了什么现象（例如：点击 EnumPort 找不到串口、点击 Read ChipInfo 报错等）。
4. **电脑系统环境：** 请注明您的操作系统是 **Windows 系统**（如 Win10 / Win11）还是 **iOS/macOS 系统**（苹果系统）。
5. **多媒体附件（强烈推荐）：** 如果可以的话，**请附上报错界面的高清截图或操作过程的短视频**，这样能让我们更快速地为您定位并解决问题。

* **📧 电子邮箱：** [您的工作或客服邮箱]
* **🌐 公司官网：** [深圳大夏龙雀科技官网链接]
* **🏢 公司名称：** 深圳大夏龙雀科技有限公司 (Shenzhen DX-SMART Technology Co., Ltd.)
